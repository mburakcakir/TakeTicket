package com.mburakcakir.taketicket.util

enum class Status {
    SUCCESS,
    ERROR,
    LOADING
}

enum class EntryState {
    USERNAME,
    PASSWORD,
    EMAIL
}

enum class EntryType {
    LOGIN,
    REGISTER
}